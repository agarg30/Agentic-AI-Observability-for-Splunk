# Third-party dependencies for Agentic AI Observability for Splunk
#
# Install with:
#   pip install -r requirements.txt
#
# Requires Python 3.10+

# MCP Python SDK — Model Context Protocol client (SSE/streamable-HTTP transport)
mcp>=1.1.0

# Async HTTP client — used by the MCP client for HTTPS connections to Splunk MCP Server
httpx>=0.27.0
